# CHATGPT.md

*[English version](CHATGPT.en.md)*

ChatGPT/Codexは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。ChatGPT/CodexはAGENTS.mdをネイティブに読み込むため、本ファイルは主にChatGPTとの連携方法の案内として用いる。

ChatGPT/Codexは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## ChatGPTとの連携方法

「常に」連携させるには、会話ごとの手動操作に頼らない仕組みが必要である。利用している場面（ChatGPT / Codex CLI）によって設定場所が異なるため、それぞれ説明する。

### 方法A：ChatGPT（chatgpt.com）向け

**設定 → パーソナライズ（Personalization） → カスタム指示（Custom Instructions）** に、本リポジトリの参照を指示する一文を書いておく方法が最も確実である。ここに書いた内容はすべての新しい会話に自動的に適用されるため、コネクタのように毎回オンにする手間がない。

例：

> 研究室関連の作業（論文・レポート執筆、コーディング、資料作成など）を行う際は、`<GitHubユーザー名>/AI_GUIDE` リポジトリの内容（特にCHATGPT.mdとdocs/rules.md）を指示書として扱ってください。GitHubコネクタが有効な場合は直接参照し、無効な場合はその旨を伝えてください。

これだけで、会話のたびにコネクタを設定し忘れても「参照すべきものがある」という前提だけは常にChatGPTに伝わる。ただし、実際にリポジトリの最新内容を読み込むには、その会話でGitHubコネクタも別途有効にする必要がある点は変わらない。

GitHubコネクタ自体は、以下の手順で利用できる。

1. ChatGPTで 設定 → Connectors（コネクタ） を開く。
2. GitHubコネクタを追加し、GitHubアカウントで認証する。
3. アクセスを許可するリポジトリとして、対象リポジトリ（本リポジトリを含む）を選択する。
4. 会話内でこのコネクタを参照すると、ChatGPTがリポジトリの内容をその都度取得できる。

注意点：

- コネクタ機能はPlus以上の有料プラン（Plus/Pro/Team/Enterprise等）向けに提供されている（無料プランでの利用可否は未確認）。Enterprise/Eduプランでは既定で無効になっており、管理者が組織単位で有効化する必要がある。
- カスタム指示・コネクタとも、会話ごとの参照が保証されるわけではなく、明示的に参照を指示した方が確実な場合がある。

### 方法B：Codex CLI / Codexに自動的に読み込ませる（常時連携として最も確実）

Codex CLIは、Claude CodeやGemini CLIと異なり、`@ファイルパス` によるインポート構文をサポートしていない。そのため、グローバル設定ファイルには実際の内容そのものをコピーして配置する必要がある。

1. 本リポジトリをローカルにクローンする（`git clone https://github.com/<GitHubユーザー名>/AI_GUIDE.git`）。
2. グローバル設定ファイル `~/.codex/AGENTS.md`（`$CODEX_HOME`環境変数で変更可能。既定は `~/.codex`）に、クローンしたリポジトリの `AGENTS.md` の内容をコピーして貼り付ける。ファイルが存在しない場合は新規作成する。
3. Codex CLI / Codexは、セッションごとにグローバルの `~/.codex/AGENTS.md` と、作業中のプロジェクトの `AGENTS.md`（存在する場合）を両方自動的に読み込み、コンテキストに結合する。
4. 本リポジトリの内容を更新した場合は、`git pull` した上で、`~/.codex/AGENTS.md` の内容を手動で最新の内容に置き換える必要がある（自動同期はされない）。

常時連携が目的であれば方法Bが最も確実である。ただし、リポジトリの更新が自動反映されない点には注意すること。ブラウザ上のChatGPTでも同じ内容を都度参照したい場合は、方法Aを補完手段として併用するとよい。
