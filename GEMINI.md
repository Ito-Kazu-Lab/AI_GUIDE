# GEMINI.md

*[English version](GEMINI.en.md)*

Gemini CLIは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Gemini CLIは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## 利用条件

- 学習機能をオフにして利用すること。

## Geminiとの連携方法

「常に」連携させるには、会話ごとの手動操作に頼らない仕組みが必要である。利用している場面（Web/モバイル/クラウドでの作業：Gemini（gemini.google.com） / ローカルでの作業：Gemini CLI）によって設定場所が異なるため、常時GitHubと連携する方法をそれぞれ説明する。

### 方法A：常時GitHubと連携する方法（Web/モバイル/クラウドでの作業向け）

Gemini（gemini.google.com / モバイルアプリ）を対象とする。

**設定と機能（Settings & help） → Personal Intelligence → 「Geminiへの指示（Instructions for Gemini）」** に、本リポジトリの参照を指示する一文を書いておくと、すべての新しい会話に自動的に適用される（この機能は以前「保存された情報（Saved info）」という名称だったため、UIによって表記が異なる場合がある）。

例：

> 研究室関連の作業（論文・レポート執筆、コーディング、資料作成など）を行う際は、`<GitHubユーザー名>/AI_GUIDE` リポジトリの内容（特にGEMINI.mdとdocs/rules.md）を指示書として扱ってください。

**重要な制約：Gemini（gemini.google.com / アプリ）には、GitHubリポジトリを接続できる公式のコネクタ・拡張機能が現時点で存在しない。** Googleの公式サポートページに掲載されている連携可能なサービス一覧（Google Photos、YouTube、Google Workspace各種、Google検索関連、連絡先）にGitHubは含まれていない。そのため、上記の「Geminiへの指示」を設定しても、Gemini自身がリポジトリの最新内容を直接取得することはできない。

リポジトリの内容を都度参照させたい場合は、該当ファイルの内容をチャットに直接貼り付ける方法が代替手段となる。この方法は会話のたびに貼り付けが必要になる点に注意する。

### 方法B：常時GitHubと連携する方法（ローカルでの作業向け）

ローカル環境でのGemini CLIによる作業を対象とする。Gemini CLIに自動的に読み込ませる方法であり、常時連携として最も確実である。

プロジェクトごとに `GEMINI.md` を置く方法もあるが、**グローバル設定ファイル**に本リポジトリを読み込む設定をしておくと、どのプロジェクトで作業していても常に自動的に読み込まれるため、より確実である。

1. 本リポジトリをローカルにクローンする（`git clone https://github.com/<GitHubユーザー名>/AI_GUIDE.git`）。
2. グローバル設定ファイル `~/.gemini/GEMINI.md`（Windowsの場合 `C:\Users\<ユーザー名>\.gemini\GEMINI.md`）に、クローンしたリポジトリの `AGENTS.md` を `@` インポート構文で1行追記する。存在しない場合は新規作成する（このインポート構文は `.md` ファイルのみに対応している）。

   ```
   @<クローンしたリポジトリの絶対パス>/AGENTS.md
   ```

3. Gemini CLIはセッションごとにグローバル `GEMINI.md` を自動的に読み込み、`@` で指定したファイルの内容を展開してコンテキストに含めるため、作業中のプロジェクトを問わず、明示的な操作なしに毎回本リポジトリの内容が読み込まれる。実際に読み込まれた内容は `/memory show` コマンドで確認できる。
4. 本リポジトリの内容を更新したい場合は `git pull` するだけで、次回セッションから最新内容が反映される（既存セッション内であれば `/memory reload` で再読み込みできる）。

常時連携が目的であれば方法Bが最も確実である。方法Aは、Gemini自身がリポジトリを直接参照できないという制約があるため、あくまで「参照すべきものがある」という前提を伝える補助的な位置づけとなる。
