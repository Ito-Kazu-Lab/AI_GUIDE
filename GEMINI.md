# GEMINI.md

*[English version](GEMINI.en.md)*

Gemini CLIは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Gemini CLIは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## Geminiとの連携方法

利用しているツール（Gemini / Gemini CLI）によって、設定方法が異なる。

### 方法A：Gemini（gemini.google.com）で都度参照する

Gemini appの拡張機能（Extensions）を通じて外部サービスを参照できる場合があるが、GitHubリポジトリを恒常的に接続する公式コネクタは現時点では提供されていない。リポジトリの内容を都度参照させたい場合は、該当ファイルの内容をチャットに直接貼り付ける方法が代替手段となる。この方法は会話のたびに貼り付けが必要になる点に注意する。

### 方法B：Gemini CLIに自動的に読み込ませる（常時連携として最も確実）

1. 本リポジトリをローカルにクローンする（`git clone https://github.com/<GitHubユーザー名>/AI_GUIDE.git`）。
2. 作業対象のプロジェクトのルートに、本ファイル（`GEMINI.md`）またはその要約を配置する。
3. Gemini CLIはセッションごとに `GEMINI.md` を自動的に読み込むため、明示的な読み込み操作なしに毎回コンテキストに含まれる。
4. 本リポジトリの内容を更新したい場合は、`git pull` で最新化する。

常時連携が目的であれば方法Bが最も確実である。ブラウザ上のGeminiでも同じ内容を都度参照したい場合は、方法Aを補完手段として併用するとよい。
