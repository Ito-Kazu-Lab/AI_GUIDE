# GEMINI.md

*[English version](GEMINI.en.md)*

Gemini CLIは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Gemini CLIは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## Geminiとの連携方法

今使っているツールによって、設定方法が変わります。

### 方法A：Gemini（gemini.google.com）で都度参照する

Gemini appの拡張機能（Extensions）を通じて外部サービスを参照できる場合がありますが、GitHubリポジトリを恒常的に接続する公式コネクタは現時点では提供されていません。リポジトリの内容を都度参照させたい場合は、該当ファイルの内容をチャットに直接貼り付ける方法が代替手段となります。この方法は会話のたびに貼り付けが必要になる点に注意してください。

### 方法B：Gemini CLIで「毎回自動的に」読み込ませる（確実な方法）

1. リポジトリをローカルにクローンする（`git clone https://github.com/Ito-Kazu-Lab/AI_GUIDE.git`）
2. そのフォルダ（またはあなたの作業プロジェクトのルート）に `GEMINI.md`（本ファイル）を置き、AI_GUIDEの要点・ルールを記載しておく
3. Gemini CLIはセッションごとにGEMINI.mdを自動で読み込むため、明示的に読み込ませる操作なしで毎回コンテキストに入る
4. リポジトリ自体を更新したい時は `git pull` するだけで最新化できる

普段の作業でGemini CLIを使う場合は、方法Bが最も確実です。ブラウザ上のGeminiでも同じ内容を参照したい場合は、方法Aの代替手段を併用してください。
