# CLAUDE.md

*[English version](CLAUDE.en.md)*

Claude Codeは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Claude Codeは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## Claudeとの連携方法

「常に」連携させるには、会話ごとの手動操作に頼らない仕組みが必要である。利用している場面（claude.aiでのチャット全般 / Claude Code）によって設定場所が異なるため、それぞれ説明する。

### 方法A：claude.aiでのチャット全般（Web/モバイル）向け

**設定 → プロフィール → 「Claudeに知っておいてほしい個人的な好み」** に、本リポジトリの参照を指示する一文を書いておく方法が最も確実である。ここに書いた内容はすべての新しい会話に自動的に読み込まれるため、コネクタのトグルのように毎回オンにする手間がない。

例：

> 研究室関連の作業（論文・レポート執筆、コーディング、資料作成など）を行う際は、`<GitHubユーザー名>/AI_GUIDE` リポジトリの内容（特にCLAUDE.mdとdocs/rules.md）を研究室の指示書として扱ってください。GitHubコネクタが有効な場合は直接参照し、無効な場合はその旨を伝えてください。

これだけで、会話のたびにコネクタをオンにし忘れても「参照すべきものがある」という前提だけは常にClaudeに伝わる。ただし、実際にリポジトリの最新内容を読み込むには、その会話でGitHubコネクタも別途オンにする必要がある点は変わらない。

GitHubコネクタ自体は、GitHubが公式にホストするリモートMCPサーバーをカスタムコネクタとして登録することで利用できる。

1. GitHub側の準備は不要（OAuth認証のみ）。
2. claude.aiで 設定 → コネクタ → カスタムコネクタを追加する。
3. URLに `https://api.githubcopilot.com/mcp/` を入力する。
4. GitHubアカウントで認証し、スコープを承認する（本リポジトリを含む対象リポジトリへのアクセスを許可する）。
5. 会話内でこのコネクタをオンにすると、Claudeがリポジトリの内容をその都度取得できる。

注意点：

- カスタムコネクタはPro以上の有料プランでのみ利用可能。
- コネクタは会話ごとにオン/オフを切り替える仕組みであり、自動的に常時オンにはならない。新しい会話を始めるたびにオンにする必要がある。

### 方法B：Claude Code向け

プロジェクトごとに `CLAUDE.md` を置く方法もあるが、**グローバル設定ファイル**に本リポジトリを読み込む設定をしておくと、どのプロジェクトで作業していても常に自動的に読み込まれるため、より確実である。

1. 本リポジトリをローカルにクローンする（`git clone https://github.com/<GitHubユーザー名>/AI_GUIDE.git`）。
2. グローバル設定ファイル `~/.claude/CLAUDE.md`（Windowsの場合 `C:\Users\<ユーザー名>\.claude\CLAUDE.md`、macOS/Linuxの場合 `/home/<ユーザー名>/.claude/CLAUDE.md` など）に、クローンしたリポジトリの `AGENTS.md` を `@` インポート構文で1行追記する。存在しない場合は新規作成する。

   ```
   @<クローンしたリポジトリの絶対パス>/AGENTS.md
   ```

   例（Windowsの場合）：

   ```
   @C:\Users\<ユーザー名>\path\to\AI_GUIDE\AGENTS.md
   ```

3. Claude Codeはセッションごとにグローバル `CLAUDE.md` を自動的に読み込み、`@` で指定したファイルの内容を展開してコンテキストに含めるため、作業中のプロジェクトを問わず、明示的な操作なしに毎回本リポジトリの内容が読み込まれる。
4. 本リポジトリの内容を更新したい場合は `git pull` するだけで、次回セッションから最新内容が反映される。

### まとめ

| 場面 | 設定場所 | 効果 |
| --- | --- | --- |
| claude.aiでのチャット全般 | 設定 → プロフィール → 個人的な好み | 全会話で自動的に参照の指示が読み込まれる（内容取得にはコネクタを別途オンにする必要がある） |
| Claude Code | グローバル設定ファイル `~/.claude/CLAUDE.md`（`@`インポート） | 作業中のプロジェクトを問わず、セッションごとに内容が自動的に読み込まれる |

両方を設定しておくと、「研究室関連の作業をする時は常に本リポジトリを踏まえる」という状態に近づく。
