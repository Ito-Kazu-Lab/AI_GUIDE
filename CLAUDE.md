# CLAUDE.md

*[English version](CLAUDE.en.md)*

Claude Codeは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Claude Codeは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## Claudeとの連携方法

今使っているツールによって、設定方法が変わります。

### 方法A：claude.aiでカスタムコネクタとして常時連携（チャット全般で使いたい場合）

GitHubは「ワンクリックで追加できる公式ディレクトリアプリ」にはまだ入っていませんが、GitHub自身がホストしている公式リモートMCPサーバーをカスタムコネクタとして登録できます。

1. GitHub側の準備は不要（OAuth認証のみ）
2. claude.aiで 設定 → コネクタ → カスタムコネクタを追加
3. URLに `https://api.githubcopilot.com/mcp/` を入力
4. GitHubアカウントで認証し、スコープを承認（`Ito-Kazu-Lab/AI_GUIDE` を含む対象リポジトリへのアクセスを許可）
5. 各会話でこのコネクタをオンにすると、Claudeがリポジトリの中身をその都度取得できます

注意点が2つあります。

- カスタムコネクタはPro以上の有料プラン限定の機能です
- 「常に連携」といっても、コネクタは会話ごとにオン/オフを切り替える仕組みなので、新しい会話を始めるたびにトグルをオンにする一手間は残ります（自動で毎回オンにはなりません）

### 方法B：Claude Codeで「毎回自動的に」読み込ませる（すでに導入済みなので、こちらが確実）

以前Claude Codeをセットアップ済みとのことなので、こちらの方が「常に連携」という目的には合っていると思います。

1. リポジトリをローカルにクローン（`git clone https://github.com/Ito-Kazu-Lab/AI_GUIDE.git`）
2. そのフォルダ（またはあなたの作業プロジェクトのルート）に `CLAUDE.md` を置き、AI_GUIDEの要点・ルールを記載しておく
3. Claude CodeはセッションごとにCLAUDE.mdを自動で読み込むため、明示的に読み込ませる操作なしで毎回コンテキストに入ります
4. リポジトリ自体を更新したい時は `git pull` するだけで最新化できます

普段Wordでの執筆やドキュメント整理にClaude Codeを使いたいとのことでしたので、B案でCLAUDE.mdにAI_GUIDEの内容（執筆ルール、命名規則など）を要約しておくと、毎回自動的に参照される状態が作れます。A案はブラウザ上のチャットでも同じリポジトリを見たい場合の補完として併用するとよいでしょう。

どちらを軸にするか決まっていれば、具体的なCLAUDE.mdの書き方やコネクタの認証手順をもう少し詳しく手伝えます。
