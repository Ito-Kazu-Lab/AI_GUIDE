# CLAUDE.md

*[English version](CLAUDE.en.md)*

Claude Codeは、本ファイルを案内として参照し、詳細な指示・ドキュメント構成については [AGENTS.md](AGENTS.md) を参照してください。

Claude Codeは、このリポジトリ内での作業前だけでなく、いかなる作業前にも、必ず [AGENTS.md](AGENTS.md) の内容に従ってください。

## Claudeとの連携方法

利用しているツール（claude.ai / Claude Code）によって、設定方法が異なる。

### 方法A：claude.aiでカスタムコネクタとして連携する（ブラウザでのチャット利用向け）

GitHubは「ワンクリックで追加できる公式ディレクトリアプリ」には含まれていないが、GitHub自身がホストする公式リモートMCPサーバーをカスタムコネクタとして登録できる。

1. GitHub側の準備は不要（OAuth認証のみ）。
2. claude.aiで 設定 → コネクタ → カスタムコネクタを追加する。
3. URLに `https://api.githubcopilot.com/mcp/` を入力する。
4. GitHubアカウントで認証し、スコープを承認する（本リポジトリを含む対象リポジトリへのアクセスを許可する）。
5. 各会話でこのコネクタをオンにすると、Claudeがリポジトリの内容をその都度取得できる。

注意点：

- カスタムコネクタはPro以上の有料プランでのみ利用可能。
- コネクタは会話ごとにオン/オフを切り替える仕組みであり、自動的に常時オンにはならない。新しい会話を始めるたびにオンにする必要がある。

### 方法B：Claude Codeに自動的に読み込ませる（常時連携として最も確実）

1. 本リポジトリをローカルにクローンする（`git clone https://github.com/<GitHubユーザー名>/AI_GUIDE.git`）。
2. 作業対象のプロジェクトのルートに、本ファイル（`CLAUDE.md`）またはその要約を配置する。
3. Claude Codeはセッションごとに `CLAUDE.md` を自動的に読み込むため、明示的な読み込み操作なしに毎回コンテキストに含まれる。
4. 本リポジトリの内容を更新したい場合は、`git pull` で最新化する。

常時連携が目的であれば方法Bが最も確実である。方法Aは、ブラウザ上のチャットでも同じリポジトリを都度参照したい場合の補完手段として併用するとよい。
